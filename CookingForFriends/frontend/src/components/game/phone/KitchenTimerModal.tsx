/** Legacy kitchen timer modal.
 *
 * Cooking timers are now non-blocking and derived from activeCookingSteps.
 * Keeping this component as a no-op avoids reintroducing a second timer state.
 */

export default function KitchenTimerModal() {
  return null
}
